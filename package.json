{
  "name": "pi-diy-gate",
  "version": "0.1.0",
  "description": "Pi extension that occasionally asks users to solve suitable requests themselves.",
  "type": "module",
  "keywords": ["pi-package", "pi-extension"],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "rm -rf .tmp-build && tsc --outDir .tmp-build --noEmit false && node --test test/*.test.mjs"
  },
  "pi": {
    "extensions": ["./extensions/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "file:/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
